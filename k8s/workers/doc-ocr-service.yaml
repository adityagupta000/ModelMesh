apiVersion: v1
kind: Service
metadata:
  name: doc-ocr-worker
  namespace: modelmesh
spec:
  selector: { app: doc-ocr-worker }
  ports:
    - name: http
      port: 8001
      targetPort: 8001
    - name: grpc
      port: 50051
      targetPort: 50051
